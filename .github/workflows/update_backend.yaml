name: every-12-hrs-cron
on:
  schedule:
    - cron: "30 12 * * *"
  workflow_dispatch:
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Force update backend data
        run: curl --request GET --url https://cf-download-monitor.vercel.app/api/update_tracked_projects_backend